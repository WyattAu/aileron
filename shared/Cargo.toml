[package]
name = "aileron-shared"
version = "0.1.0"
edition = "2024"
description = "Shared types between Aileron native backend and Leptos WASM chrome"
license = "Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
uuid = { version = "1", features = ["v4", "serde", "js"] }
url = "2"
